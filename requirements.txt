# LiftDetect backend dependencies
# Install with: pip install -r requirements.txt

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9    # required for file uploads

# CV / ML (heavy — needs CUDA GPU for real inference)
numpy>=1.24,<2.0
opencv-python>=4.9.0
ultralytics>=8.1.0          # YOLOv8
deep-sort-realtime>=1.3.2   # DeepSort tracker

# MMPose / MMDet (install separately — see README)
# mmcv>=2.0.0
# mmdet>=3.0.0
# mmpose>=1.3.0

# Note for reviewers without a GPU:
# The engine auto-detects missing models and falls back to DEMO mode.
# You only need fastapi, uvicorn, python-multipart, numpy, opencv-python
# to run the dashboard with synthetic data.
